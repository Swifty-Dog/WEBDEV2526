{
  "private": true,
  "workspaces": ["FE"],
  "scripts": {
    "install-all": "npm install",
    "frontend": "npm run dev -w FE",
    "backend": "dotnet run --project BE/OfficeCalendar.API/OfficeCalendar.API.csproj",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
